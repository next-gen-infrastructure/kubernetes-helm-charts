apiVersion: {{ include "common.capabilities.daemonset.apiVersion" . }}
kind: DaemonSet
metadata:
  name: {{ template "common.names.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{ include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
  annotations:
  {{ if .Values.env.vaultSecret }}
    reloader.stakater.com/auto: "true"
  {{- end }}
  {{- if .Values.commonAnnotations }}
    {{- include "common.tplvalues.render" ( dict "value" .Values.commonAnnotations "context" $ ) | nindent 4 }}
  {{- end }}
spec:
  revisionHistoryLimit: 2
  selector:
    matchLabels: {{ include "common.labels.matchLabels" (dict "customLabels" .Values.podLabels "context" $ ) | nindent 6 }}
  template:
    metadata:
      labels: {{ include "common.labels.standard" (dict "customLabels" .Values.podLabels "context" $ ) | nindent 8 }}
      {{- if or .Values.podAnnotations .Values.metrics.enabled }}
      annotations: {{ include "common.annotations.podAnnotations" . | trim | nindent 8 }}
      {{- end }}
    spec:
      {{- if .Values.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      {{- end }}
      {{- if .Values.podSecurityContext }}
      securityContext: {{ toYaml .Values.podSecurityContext | nindent 8 }}
      {{- end }}
      {{- if .Values.affinity }}
      affinity: {{- include "common.tplvalues.render" (dict "value" .Values.affinity "context" $) | nindent 8 }}
      {{- else }}
      affinity:
        {{- if .Values.podAffinityPreset }}
        podAffinity: {{- include "common.affinities.pods" (dict "type" .Values.podAffinityPreset "context" $) | nindent 10 }}
        {{- end }}
        {{- if .Values.podAntiAffinityPreset }}
        podAntiAffinity: {{- include "common.affinities.pods" (dict "type" .Values.podAntiAffinityPreset "context" $) | nindent 10 }}
        {{- end }}
        {{- if .Values.nodeAffinityPreset.type }}
        nodeAffinity: {{- include "common.affinities.nodes" (dict "type" .Values.nodeAffinityPreset.type "key" .Values.nodeAffinityPreset.key "values" .Values.nodeAffinityPreset.values) | nindent 10 }}
        {{- end }}
      {{- end }}
      {{- if .Values.nodeSelector }}
      nodeSelector: {{- include "common.tplvalues.render" (dict "value" .Values.nodeSelector "context" $) | nindent 8 }}
      {{- end }}
      {{- if .Values.tolerations }}
      tolerations: {{- include "common.tplvalues.render" (dict "value" .Values.tolerations "context" $) | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "common.serviceAccount.name" . }}
      {{- if .Values.priorityClassName }}
      priorityClassName: {{ .Values.priorityClassName | quote }}
      {{- end }}
      {{- if .Values.initContainers }}
      initContainers:
        {{- range .Values.initContainers }}
        {{- $values := dict "Values" . "Chart" $.Chart "Release" $.Release "Template" $.Template -}}
        {{- $name := required "nameOverride is required for the init containers" $values.Values.nameOverride }}
        {{- $_ := set $values.Values "global" $.Values.global -}}
        {{- $__ := set $values.Values "volumes" ($values.Values.volumes | default $.Values.volumes) -}}
        {{- include "common.containers.container" $values | nindent 8 }}
        {{- end }}
      {{- end }}
      containers:
        {{- include "common.containers.container" . | nindent 8 }}
        {{/* Process sidecar containers */}}
        {{- if .Values.sidecarContainers }}
        {{- range .Values.sidecarContainers }}
        {{/* Define configuration for subobjects (containers) with inputs from root($) context*/}}
        {{- $values := dict "Values" . "Chart" $.Chart "Release" $.Release "Template" $.Template -}}
        {{- $name := required "nameOverride is required for the sidecar containers" $values.Values.nameOverride }}
        {{- $_ := set $values.Values "global" $.Values.global -}}
        {{- $__ := set $values.Values "volumes" ($values.Values.volumes | default $.Values.volumes) -}}
        {{- include "common.containers.container" $values | nindent 8 }}
        {{- end }}
        {{- end }}
      volumes: {{- include "common.volume.definitions" . | nindent 8 }}
      {{- if .Values.persistence.enabled }}
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "common.names.fullname" . }}
      {{- end }}
  {{- if .Values.volumes }}
  {{ $claimsList := list }}
  {{- range $key, $value :=.Values.volumes }}
  {{- if $value.storage }}
    {{ $claimsList := append $claimsList $key }}
  {{- end }}
  {{- end }}
  {{- if ne (len $claimsList ) 0 }}
  volumeClaimTemplates: {{ include "common.volume.claims" . | nindent 4 }}
  {{- end }}
  {{- end }}
